# 视频加载性能对比测试：优化前（moov-at-tail） vs faststart 后

> 测试日期：2026-09-22
> 测试对象：本地 `public/videos/` 下 7 个代表性活跃视频（均有 `.orig.bak.mp4` 原版备份用于对照）
> 测试方法：Python 解析 MP4 `stbl` 原子，读取「首个视频 sample 在文件内的字节偏移 + 大小」即为「浏览器边下边播时解出首帧所需下载到的最小连续字节」；按带宽换算首帧时间。
> 测试**未修改任何文件**，仅读取原版备份与主文件。

## 一、为什么这是关键对比

faststart 的本质是把 `moov` 原子从文件尾移到文件头。浏览器要播放视频必须先读 `moov`（索引）。
- **优化前（moov 在尾）**：浏览器拿不到索引，行为取决于服务器是否支持 HTTP range。
- **优化后（moov 在头）**：浏览器顺序下载到首个关键帧即可出首帧，无需等待整文件。

## 二、对比数据表

| 视频 | 文件大小 | 优化后首帧字节 | 优化前首帧(最坏/整文件) | 优化前首帧(现实/range) | 优化后@1Mbps | 优化前最坏@1Mbps | 优化前现实@1Mbps | 提速(最坏) | 提速(现实) |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| demo/HeroV5.mp4 | 86.5 MB | 0.33 MB | 86.5 MB | 0.32 MB | 0.6 s | 91.0 s | 0.6 s | 140.8x | 1.0x |
| overview/kitchen-v3.mp4 | 39.3 MB | 0.03 MB | 39.3 MB | 0.08 MB | 0.3 s | 41.6 s | 0.4 s | 124.2x | 1.1x |
| visualizations/sample-01.mp4 | 40.6 MB | 0.25 MB | 40.6 MB | 0.25 MB | 0.6 s | 42.8 s | 0.6 s | 76.9x | 1.0x |
| visualizations/sample-03.mp4 | 33.6 MB | 0.23 MB | 33.6 MB | 0.23 MB | 0.5 s | 35.5 s | 0.5 s | 66.0x | 1.0x |
| multimodal/Visual.mp4 | 23.9 MB | 0.14 MB | 23.9 MB | 0.14 MB | 0.4 s | 25.4 s | 0.4 s | 56.8x | 1.0x |
| multimodal/Motion.mp4 | 20.7 MB | 0.26 MB | 20.7 MB | 0.26 MB | 0.6 s | 22.0 s | 0.6 s | 38.4x | 1.0x |
| tasks/06.mp4 | 7.9 MB | 0.09 MB | 7.9 MB | 0.09 MB | 0.4 s | 8.6 s | 0.4 s | 21.6x | 1.0x |

## 三、两种模型说明

- **最坏模型**：服务器不支持 range、或浏览器强制顺序下载。moov 在文件尾且无索引，浏览器须下完整文件才出首帧。
  - 首帧所需总字节：优化前 **252.5 MB → 优化后 1.3 MB**（减少 99.5%），平均提速 **75.0x**。
- **现实模型**：GitHub Pages 支持 HTTP range。浏览器发 range 请求跳到文件尾取 `moov`，再从头顺序播放首帧。
  - 首帧所需总字节：优化前 **1.4 MB → 优化后 1.3 MB**（减少 1.9%），平均提速 **1.0x**。

## 四、核心结论（重要）

1. **在 GitHub Pages（支持 range）上，faststart 对「首帧出现时间」几乎没有提升（约 1.0x）。** 因为浏览器本来就能通过 range 尾部取 moov、再从头播首帧。之前的「慢网 30–87 秒首帧」估算，对 moov-at-tail 在 Pages 上是偏悲观的。
2. **faststart 的真实价值不在首帧速度，而在：**
   - **稳健性**：在不支持 range 的网络/服务器、老旧移动端浏览器下仍能流式播放（最坏模型下 91s→0.6s）。
   - **标准合规**：所有流媒体/HLS 最佳实践都要求 faststart。
   - **seek 与后续缓冲更可预测**：拖动进度条、视频继续播放时的缓冲行为更顺。
3. **「加载慢」的真正瓶颈仍是文件体积本身。** 即使首帧 0.6s 出现，`demo/HeroV5.mp4`（86.5 MB）在 1 Mbps 下要把整文件缓冲完仍需约 87s，中间必然卡顿。首帧字节已极小（1.3 MB），能压缩的空间在**总码率/分辨率**，而非 faststart。

## 五、对下一步的建议

- **faststart 这一步已正确完成且必要**，应保留（稳健性 + 标准）。但它单独**不会**让你在 Pages 上感觉"视频变快"。
- **要真正改善「加载慢」体感，必须进入压缩阶段**：将 DEMO / OVERVIEW / RECON 大头视频降到 1920×1080、码率 4–5 Mbps（H.264 不变），预计 DEMO 86→~22 MB、OVERVIEW 39→~13 MB、sample-01 40→~9 MB。这才是首帧与流畅度的双重提升来源。
- **验证建议**：在真实 Pages 上用浏览器 DevTools → Network 面板，观察视频请求的 `started → first byte → 首帧渲染` 时间，可直接验证「现实模型」结论。

## 六、方法论局限

- 首帧时间为「字节量 / 带宽 + TTFB(0.3s)」估算，非真实浏览器实测；TTFB 与带宽均为假设值。
- 现实模型假设 GitHub Pages 支持 range（事实如此）且浏览器行为标准；个别老旧客户端可能偏离。
- 未计入「播放流畅度」：首帧之后的连续缓冲仍取决于总文件大小与带宽。
